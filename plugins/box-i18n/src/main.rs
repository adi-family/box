//! @box/i18n plugin — translation/localization kind provider.
//!
//! Owns the `i18n` kind and the `t"key"` value form's resolution.
//! Runs as a standalone binary; communicates with the Box host over
//! JSON-RPC 2.0 on stdio.

fn main() {}
